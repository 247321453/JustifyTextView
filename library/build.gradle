apply plugin: 'com.android.library'
version = '1.1.5'

android {
    compileSdkVersion 23
    buildToolsVersion "23.0.2"

    defaultConfig {
        minSdkVersion 14
        targetSdkVersion 23
        versionCode 20160224
        versionName version
    }
}
dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
}
